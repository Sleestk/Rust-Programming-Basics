// Attributes - metadata for the compiler
#![allow(unused)]
// main() is the entry point of Rust programs
fn main() {
    // Macros in Rust generate code at compile time and are
    // invoked with an exlamation mark (!)
    // println! is a mcaro that prints text to the console
    println!("Hello, world!");
}
